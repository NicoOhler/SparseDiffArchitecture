defaults:
    - _self_
    - general : sample_custom
    - model : model_custom
    - train : train_custom
    - dataset : custom

hydra:
  job:
    chdir: True
  run:
    dir: ../outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}-${general.name}
